import { RevealRule } from '@/components/motion/RevealRule';

/**
 * Full-bleed hairline divider used between sections. Draws itself in as the
 * section it opens comes into view.
 */
export function Rule() {
  return <RevealRule />;
}
